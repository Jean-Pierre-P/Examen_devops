const test = require("node:test");
const assert = require("node:assert");

// Test mínimo de ejemplo. Reemplázalo o amplíalo con tests reales
// de tus endpoints (usando supertest, por ejemplo) para que la etapa
// "test" del pipeline tenga contenido real que demostrar en la defensa.
test("suma básica de control", () => {
  assert.strictEqual(1 + 1, 2);
});
